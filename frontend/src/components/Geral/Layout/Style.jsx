import styled from "styled-components";
import { Variables } from "../../Geral/Styles/Variables";

export const SLayout = styled.div`
    display: flex;
`

export const SMain = styled.main`
    padding: calc(${Variables.smSpacing * 2});
    width: 100%;
`